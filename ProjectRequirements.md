## 后端需求
* [x] 抓取新闻，将标题和内容区分开，目前是哈希的模式在存储
* [x] 请求路由打通
* [x] 请求参数的全局校验打通
* [ ] 数据库连接
* [ ] 数据库用户和数据库安全
* [ ] 数据库数据结构
* [ ] 设计 token 算法，每个请求都应该有一个动态的 token 校验。
* [ ] web 安全，主要是对每个用户，每个 http 请求做校验，这里可以考虑做的复杂一些，可能会出现性能的浪费，没关系，因为不是面向市场的
* [ ] 安全上可以使用非对称加密的方案，或者多重 token 应该就可以满足了
* [ ] IP 的洪水攻击。一旦出现 ip 多次请求那么立即将这个 IP 封禁，加入黑名单。
* [ ] 需要设计一个 IP 黑名单
* [ ] 对所有的 http 请求增加节流功能，要求是同一个 ip 下发的同一个请求，2s 内不能再发。限制请求。
* [ ] 数据监控，每个用户在搜索的时候，哪些接口是使用最多的
* [ ] 数据库的定时备份。
* [ ] 更新数据的时候，也要有数据备份。能数据进行回滚。
* [ ] 后端的分词算法，要使用 Nodejieba 还是起他的分词库，又或者自己写一个算法。
* [ ] 多人实时更新数据的时候，应该怎么去控制更新。这是一个新的需求

## 前端需求
* [ ] 搭建基础架构
* [ ] web 安全，每个请求都要做请求的 token hash，这个算法是随机的，而且解密在服务器上要有对应
* [ ] 前端路由。
* [ ] 前端用户系统设计，对不同权限的用户要有不同的展示界面。
* [ ] 每日新闻的补全部分，因为抓取的是一个大概的轮廓，里面的很多细节都是需要去补充的。这个是最麻烦的，也是最核心的地方。
* [ ] 补充的时候采用的是标题下的输入和不同的 tag，name，company，等等
* [ ] 前端数据监控，设计一个监控函数，对一个页面的使用，销毁，一个功能的使用和销毁时间要做记录。
* [ ] 需要有一个任务中心，当发起异步请求的时候，要将任务中心中的任务步骤展示出来。